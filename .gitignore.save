# Environment secrets — never commit
.env

# Encrypted paper artifacts — sensitive generated files
storage/*.enc

# Node dependencies
node_modules/

# TypeScript build output
dist/

# macOS metadata
.DS_Store# Environment secrets — never commit
.env

# SQLite database — contains all user and exam data
platform.db
platform.db-shm
platform.db-wal

# Encrypted paper artifacts — sensitive generated files
storage/*.enc

# Node dependencies
node_modules/

# TypeScript build output
dist/

# macOS metadata
.DS_Store
